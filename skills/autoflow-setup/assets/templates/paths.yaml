# 路径配置
# 由 autoflow-setup 自动生成

data:
  dataset_root: ""            # 数据集根目录
  train_data: ""              # 训练数据路径
  val_data: ""                # 验证数据路径
  test_data: ""               # 测试数据路径

model:
  pretrained: ""              # 预训练模型路径
  checkpoint_dir: ""          # checkpoint 保存目录
  best_model: ""              # 最佳模型路径

output:
  log_dir: ""                 # 日志目录
  result_dir: ""              # 结果输出目录
  tensorboard_dir: ""         # TensorBoard 日志目录（如适用）

cache:
  cache_dir: ""               # 缓存目录（如适用）

custom_paths: {}              # 其他自定义路径
