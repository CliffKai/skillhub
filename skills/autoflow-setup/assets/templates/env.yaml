# 环境配置
# 由 autoflow-setup 自动生成

python:
  version: ""                 # Python 版本要求

environment:
  type: ""                    # 环境类型: conda / venv / system / docker
  name: ""                    # 环境名称
  path: ""                    # 环境路径（如适用）
  activation: ""              # 激活命令

dependencies:
  file: ""                    # 依赖文件路径 (requirements.txt / environment.yaml)
  key_packages: []            # 关键依赖包及版本

setup_commands: []            # 环境设置命令列表

verified: false               # 环境是否已验证可用
verification_date: ""         # 验证日期
